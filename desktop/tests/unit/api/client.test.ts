import { describe, it, expect } from 'vitest'

describe('API Client', () => {
  it.todo('should make GET requests with correct URL and params')
  it.todo('should make POST requests with JSON body')
  it.todo('should throw ApiError on non-OK response')
  it.todo('should perform health check with timeout')
})
